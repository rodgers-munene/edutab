import 'package:flutter/material.dart';

class HomeworkViewScreen extends StatelessWidget {
  const HomeworkViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Student Homework"),
      ),
    );
  }
}